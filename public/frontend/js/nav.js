const nav = document.getElementById('extend-nav');
const navbtn = document.getElementById('nav_response_btn');

function navappear() {
    nav.classList.toggle('active');
    navbtn.classList.toggle('active');
}